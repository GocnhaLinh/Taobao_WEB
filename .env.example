# === Backend API URL ===
# Thay URL dưới đây bằng URL backend sau khi deploy (Railway, Render, v.v...)
# Lưu ý: PHẢI có https:// ở đầu!
# VITE_API_URL=https://taobaoapi-production.up.railway.app

# Khi chạy local, dùng dòng dưới:
VITE_API_URL=http://localhost:5000
