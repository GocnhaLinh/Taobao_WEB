version: '3.8'

services:
  mongo:
    image: mongo:5.0.15
    container_name: mongodb_replica
    restart: always
    ports:
      - "27018:27017"
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
